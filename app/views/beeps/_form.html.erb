<%= form_for @beep, :html => { :multipart => true }, remote: true do |f| %>
    <%= f.label :beep %>
    <%= f.text_field :beep %>
    <%= f.label :photo %>
    <%= f.file_field :photo %>
  <%= f.submit(class: "btn btn-info") %>
<% end %>
